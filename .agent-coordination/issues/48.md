## Clippy Warnings

**Priority:** LOW
**Found by:** Rust Clippy Analyzer

### Issue

8 absurd comparisons of unsigned integers against zero in [`packages/agent/tests/integration_metrics.rs`](https://github.com/danielbodnar/ngfw.sh/blob/main/packages/agent/tests/integration_metrics.rs):

```rust
// These are always true for u64/usize:
assert!(rates.rx_rate >= 0);      // line ~231
assert!(rates.tx_rate >= 0);      // line ~232
assert!(metrics.connections.total >= 0);  // line ~254
assert!(metrics.connections.tcp >= 0);    // line ~258
assert!(metrics.connections.udp >= 0);    // line ~262
assert!(metrics.dns.queries >= 0);        // line ~285
// + 2 more
```

Clippy warning: "comparison is useless due to type limits"

### Impact

- Dead assertion paths (always pass)
- May mask real validation needs

### Solution

Replace with meaningful assertions:

```rust
// If checking for zero:
assert_eq!(rates.rx_rate, 0);

// If checking for positive:
assert!(rates.rx_rate > 0);

// Or remove entirely if the assertion adds no value
```

### Files

- [`packages/agent/tests/integration_metrics.rs`](https://github.com/danielbodnar/ngfw.sh/blob/main/packages/agent/tests/integration_metrics.rs)

### Effort

~15 minutes
