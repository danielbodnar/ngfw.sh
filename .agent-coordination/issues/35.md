## Parser Error

**Priority:** CRITICAL
**Found by:** Nushell Validator + Code Quality Analyzer

### Issue

Backslash line continuation on line 320 of [`scripts/demo-lifecycle.nu`](https://github.com/danielbodnar/ngfw.sh/blob/main/scripts/demo-lifecycle.nu#L320) causes a parser error.

```
Error: nu::parser::error
  x Invalid literal
     ,-[line 320:23]
 319 |     ^ssh -o StrictHostKeyChecking=no root@$instance.main_ip "
 320 | ,->     docker run -d \
     |         ...
     | |->     "
     : `---- unrecognized escape after '\' in string
```

### Impact

- Script cannot execute
- Blocks demo deployment

### Fix

Remove backslash continuation — use a single-line string or Nushell's raw string syntax:

```nushell
# Option 1: Single line
^ssh ... "docker run -d --name ngfw-agent --restart unless-stopped ..."

# Option 2: Nushell raw string
^ssh ... r#'docker run -d
  --name ngfw-agent
  --restart unless-stopped
  ...'#
```

### Files

- [`scripts/demo-lifecycle.nu:320`](https://github.com/danielbodnar/ngfw.sh/blob/main/scripts/demo-lifecycle.nu#L320)

### Effort

~5 minutes
