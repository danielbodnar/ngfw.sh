---
import DashboardViewerApp from "../../../components/monitoring/DashboardViewerApp.vue";
import PortalLayout from "../../../layouts/PortalLayout.astro";

const { userId } = Astro.locals.auth();
if (!userId) {
	return Astro.redirect("/sign-in");
}
---

<PortalLayout title="Security Events - NGFW.sh">
  <div class="space-y-6">
    <div>
      <h2 class="text-2xl font-semibold text-[--color-text-primary] mb-1">
        Security Events
      </h2>
      <p class="text-[--color-text-secondary]">
        Intrusion detection and security alerts
      </p>
    </div>

    <DashboardViewerApp
      dashboard-id="security-events"
      title="Security Events"
      description="Intrusion detection and security alerts"
      client:load
    />
  </div>
</PortalLayout>
