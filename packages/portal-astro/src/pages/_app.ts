import type { App } from 'vue';

export default (_app: App) => {
  // Global Vue configuration
  // Register global components, plugins, directives here

  // Example: _app.use(somePlugin)
  // Example: _app.component('GlobalComponent', GlobalComponent)
};
